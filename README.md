# using fastify autolaod + ncc for build plugin applicaiton 

> current event fastify plugin should build with ncc (see npm script `plugin-init`)

## build

* command

```code
yarn build myplugin-build
``````

## docker running

* build image

```code
docker-compose build
```

* starting

```code
docker-compose up -d
```